@props([
    'id' => 'djfd48j'
])


<label for="{{ $id }}" class="relative">
    <input type="checkbox" name="{{ $id }}" id="{{ $id }}" class="checkbox">
    <span class="material-symbols-outlined checkmark">check</span>
</label>
